# Tasks: Guided Demo (Smoke Test v4)

**Input**: Design documents from `specs/049-guided-demo/`

**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md

**Organization**: Tasks are grouped by user story to enable independent implementation and testing.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Phase 1: Core Skill Rewrite

**Purpose**: Rewrite the smoke test skill with the new guided demo architecture. This is the core deliverable that all other stories depend on.

**Goal**: Replace the literal scenario replay model with the synthesis/triage/evidence architecture.

- [x] T001 Read the current smoke test skill at `spex/extensions/spex/commands/speckit.spex.smoke-test.md` (402 lines), then rewrite the Overview and Hard Gate sections to reflect "Guided Demo" naming in user-facing output, update the description, and preserve the no-simulation hard gate
- [x] T002 Rewrite Step 1 (input parsing) in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` to read FRs and acceptance scenarios from the spec as primary input, with optional `## Smoke Test` section as priority hints (FR-001, FR-002)
- [x] T003 Write new Step 2 (Demo Plan Synthesis) in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` implementing the FR-to-observable-flow translation using keyword heuristic (FR-003, FR-004, FR-005, FR-020), grouping related FRs, excluding internal-only FRs, targeting 3-7 flows
- [x] T004 Write new Step 3 (Environment Triage) in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` implementing infrastructure probing, four-tier classification (full/partial/setup offered/manual), readiness table presentation, and user selection options with setup complexity estimates (FR-007, FR-008, FR-009, FR-010)
- [x] T005 Write new Step 4 (Demo Plan Presentation) in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` presenting the synthesized plan to the user before execution, allowing reordering/skipping of flows (FR-006)
- [x] T006 Rewrite Step 5 (Flow Execution) in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` implementing evidence-based execution with observable-only output (command output, HTTP responses, file contents, screenshots, logs), verdict recommendations with reasoning, and "verify yourself" instructions (FR-011, FR-012)
- [x] T007 Write new Step 6 (Auto-Skip Detection) in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` implementing detection of specs with no observable FRs, producing "All requirements are verified by unit tests" message and minimal SMOKE-TEST.md report (FR-013)
- [x] T008 Rewrite Step 7 (Report Generation) in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` to produce SMOKE-TEST.md with per-flow tier/evidence/verdict sections, FR coverage mapping table, internal-only FR list, and triage summary (FR-014)
- [x] T009 Update failure handling section in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` to include tier-aware investigation context (full-tier = investigate real system, partial-tier = proxy evidence issue) while preserving existing retry logic (FR-018)
- [x] T010 Update pipeline integration section in `spex/extensions/spex/commands/speckit.spex.smoke-test.md` to enforce always-interactive behavior regardless of `--ask` level, including `--ask never` (FR-019)

**Checkpoint**: Core skill rewrite complete. The guided demo can synthesize flows, triage infrastructure, present evidence, and generate reports.

---

## Phase 2: User Story 6 - Spec Template Guidance (Priority: P3)

**Goal**: Update the spec template so future specs include observable-behavior guidance in the `## Smoke Test` section.

**Independent Test**: Run `/speckit-specify` and verify the generated spec includes the updated `## Smoke Test` section with good/bad examples.

- [x] T011 [US6] Add optional `## Smoke Test` section to `.specify/templates/spec-template.md` with guidance text: "List the 3-5 most important user-observable behaviors you want to verify. Focus on what the user should see, not what the code does internally." Include contrasting bad/good examples (FR-016)

**Checkpoint**: New specs generated by `/speckit-specify` include observable-behavior guidance.

---

## Phase 3: Polish & Documentation

**Purpose**: Update all documentation references to reflect the guided demo changes.

- [x] T012 [P] Update `spex/extensions/spex/docs/help.md` to change smoke test description to "Guided Demo" and update the command reference (FR-015)
- [x] T013 [P] Update `README.md` smoke test references in workflow section and commands reference table to reflect "Guided Demo" naming (FR-015)

**Checkpoint**: All documentation reflects the guided demo changes.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Core Rewrite)**: No dependencies, start immediately
- **Phase 2 (Template)**: Independent of Phase 1, can run in parallel
- **Phase 3 (Docs)**: Depends on Phase 1 (need to know final naming/behavior)

### Within Phase 1

Tasks T001-T010 should be executed sequentially since they all modify the same file (`speckit.spex.smoke-test.md`). The order follows the skill's execution flow: overview, input parsing, synthesis, triage, presentation, execution, auto-skip, report, failure handling, pipeline integration.

### Parallel Opportunities

- T011 (template update) can run in parallel with Phase 1
- T012 and T013 (docs updates) can run in parallel with each other

---

## Implementation Strategy

### MVP First (Phase 1 Only)

1. Complete Phase 1: Rewrite core skill (T001-T010)
2. **STOP and VALIDATE**: Run `/speckit-spex-smoke-test` against a real spec and verify the guided demo works. Also spot-check SC-002 (demo completes < 5 min for 3-5 flows) and SC-003 (auto-skip < 2 sec for non-observable specs).
3. The smoke test is usable at this point even without template/doc updates

### Incremental Delivery

1. Phase 1 complete: Guided demo works for existing specs
2. Phase 2 complete: New specs get observable-behavior guidance
3. Phase 3 complete: All documentation updated

---

## Notes

- All Phase 1 tasks modify the same file, so they cannot be parallelized
- The core deliverable is a single file rewrite (~400 lines)
- Template and doc updates are small, independent changes
- No new files are created; only existing files are modified
